# 🔐 Настройка Keystore для подписи релизной сборки

## Шаг 1: Создание keystore.jks

Выполните следующую команду в корневой директории проекта:

```bash
keytool -genkey -v -keystore keystore.jks -alias dailyflow -keyalg RSA -keysize 2048 -validity 10000
```

### Параметры команды:
- `-keystore keystore.jks` - имя файла keystore
- `-alias dailyflow` - псевдоним ключа (используется в build.gradle.kts)
- `-keyalg RSA` - алгоритм шифрования
- `-keysize 2048` - размер ключа
- `-validity 10000` - срок действия в днях (~27 лет)

### Во время выполнения команды вам будет предложено ввести:
1. **Пароль хранилища (store password)** - сохраните его, он понадобится!
2. **Повтор пароля хранилища**
3. **Ваше имя и фамилия** - можно ввести любое имя
4. **Название организационного подразделения** - можно оставить пустым
5. **Название организации** - можно ввести любое
6. **Название города или населенного пункта** - можно ввести любой
7. **Название штата или области** - можно ввести любой
8. **Двухбуквенный код страны** - например, RU
9. **Подтверждение информации** - введите `yes`
10. **Пароль ключа (key password)** - обычно такой же, как пароль хранилища (можно нажать Enter)

## Шаг 2: Создание keystore.properties

Скопируйте файл `keystore.properties.example` в `keystore.properties`:

```bash
copy keystore.properties.example keystore.properties
```

Или вручную создайте файл `keystore.properties` в корне проекта со следующим содержимым:

```properties
storeFile=keystore.jks
storePassword=ваш_пароль_хранилища
keyAlias=dailyflow
keyPassword=ваш_пароль_ключа
```

**⚠️ ВАЖНО:** Замените `ваш_пароль_хранилища` и `ваш_пароль_ключа` на реальные пароли, которые вы ввели при создании keystore!

## Шаг 3: Проверка настройки

Убедитесь, что:
- ✅ Файл `keystore.jks` создан в корне проекта
- ✅ Файл `keystore.properties` создан и содержит правильные пароли
- ✅ Оба файла добавлены в `.gitignore` (не коммитятся в репозиторий)

## Шаг 4: Сборка подписанного релиза

После настройки keystore выполните:

```bash
.\gradlew.bat assembleRelease
```

Подписанный APK будет создан в: `app/build/outputs/apk/release/app-release.apk`

## 🔒 Безопасность

- **НЕ коммитьте** `keystore.jks` и `keystore.properties` в репозиторий
- Храните пароли в безопасном месте
- Сделайте резервную копию `keystore.jks` - без него вы не сможете обновлять приложение в Google Play!
- Если потеряете keystore, вам придется создать новое приложение в Google Play

## 📝 Примечания

- Файл `keystore.properties.example` можно коммитить в репозиторий (это шаблон)
- Файл `keystore.properties` НЕ должен коммититься (содержит пароли)
- Keystore должен храниться в безопасном месте с резервной копией

