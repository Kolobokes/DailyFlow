# 🚀 Инструкции по сборке DailyFlow

## 📋 Предварительные требования

### 1. Установка Android Studio
- Скачайте и установите [Android Studio](https://developer.android.com/studio) (версия 2023.1.1 или новее)
- Убедитесь, что установлен Android SDK 34
- Установите Android Build Tools 34.0.0

### 2. Настройка Java/Kotlin
- Убедитесь, что установлена JDK 8 или новее
- Kotlin 1.9.10 будет установлен автоматически через Gradle

### 3. Настройка Firebase
1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Создайте новый проект "DailyFlow"
3. Добавьте Android приложение с package name: `com.dailyflow.app`
4. Скачайте `google-services.json` и поместите в папку `app/`
5. Включите следующие сервисы:
   - Firebase Cloud Messaging
   - Firebase Analytics
   - Firebase Crashlytics

## 🔧 Настройка проекта

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/dailyflow.git
cd dailyflow
```

### 2. Открытие в Android Studio
1. Запустите Android Studio
2. Выберите "Open an existing project"
3. Выберите папку с проектом DailyFlow
4. Дождитесь синхронизации Gradle

### 3. Настройка подписи приложения
Создайте файл `app/keystore.properties`:
```properties
storeFile=dailyflow-release-key.keystore
storePassword=your_store_password
keyAlias=dailyflow
keyPassword=your_key_password
```

Создайте keystore файл:
```bash
keytool -genkey -v -keystore dailyflow-release-key.keystore -alias dailyflow -keyalg RSA -keysize 2048 -validity 10000
```

## 🏗️ Сборка проекта

### 1. Debug сборка
```bash
./gradlew assembleDebug
```
APK файл будет создан в `app/build/outputs/apk/debug/`

### 2. Release сборка
```bash
./gradlew assembleRelease
```
APK файл будет создан в `app/build/outputs/apk/release/`

### 3. Установка на устройство
```bash
./gradlew installDebug
```

## 📱 Тестирование

### 1. Запуск на эмуляторе
1. Создайте Android эмулятор в Android Studio
2. Выберите устройство с API level 24 или выше
3. Запустите приложение

### 2. Запуск на реальном устройстве
1. Включите режим разработчика на устройстве
2. Включите отладку по USB
3. Подключите устройство к компьютеру
4. Запустите приложение

## 🔒 Настройка безопасности

### 1. Проверка шифрования
Убедитесь, что SQLCipher правильно настроен:
- База данных должна быть зашифрована
- Ключи хранятся в Android Keystore

### 2. Проверка разрешений
Приложение запрашивает минимальный набор разрешений:
- `RECORD_AUDIO` - для голосового ввода
- `POST_NOTIFICATIONS` - для уведомлений
- `READ_EXTERNAL_STORAGE` - для доступа к файлам (Android 12 и ниже)
- `READ_MEDIA_IMAGES` - для доступа к изображениям (Android 13+)

## 🚀 Публикация в Google Play

### 1. Подготовка к публикации
1. Создайте AAB файл: `./gradlew bundleRelease`
2. Проверьте, что ProGuard включен для обфускации кода
3. Убедитесь, что все тесты проходят

### 2. Загрузка в Google Play Console
1. Создайте аккаунт разработчика
2. Создайте новое приложение
3. Загрузите AAB файл
4. Заполните описание, скриншоты, политику конфиденциальности

### 3. Проверка безопасности
Убедитесь, что выполнены все требования:
- ✅ Шифрование локальных данных
- ✅ Минимальный набор разрешений
- ✅ ProGuard активен
- ✅ HTTPS везде
- ✅ Политика конфиденциальности добавлена

## 🐛 Отладка

### 1. Логи
```bash
adb logcat | grep DailyFlow
```

### 2. Проверка базы данных
```bash
adb shell
run-as com.dailyflow.app
ls databases/
```

### 3. Проверка уведомлений
```bash
adb shell dumpsys notification
```

## 📊 Мониторинг

### 1. Firebase Analytics
- Отслеживайте использование приложения
- Анализируйте пользовательское поведение

### 2. Firebase Crashlytics
- Мониторьте краши приложения
- Получайте отчеты об ошибках

## 🔄 Обновления

### 1. Обновление зависимостей
```bash
./gradlew dependencyUpdates
```

### 2. Обновление версии
Измените `versionCode` и `versionName` в `app/build.gradle.kts`

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи приложения
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки Firebase
4. Создайте issue в репозитории

## 🎯 Следующие шаги

После успешной сборки:
1. Протестируйте все функции приложения
2. Проведите тестирование безопасности
3. Подготовьте скриншоты для Google Play
4. Напишите описание приложения
5. Опубликуйте в Google Play Store

---

**Удачи с вашим приложением DailyFlow! 🚀**
