# Инструкция по подготовке обновления приложения

## 📋 Шаги подготовки обновления

### 1. Обновление версии приложения

В файле `app/build.gradle.kts` обновите:

```kotlin
defaultConfig {
    applicationId = "com.dailyflow.app"
    minSdk = 24
    targetSdk = 34
    versionCode = 2        // Увеличьте на 1 (было 1)
    versionName = "1.1"     // Обновите версию (было "1.0")
    // ...
}
```

**Правила версионирования:**
- `versionCode` - целое число, должно увеличиваться с каждым обновлением (1, 2, 3, 4...)
- `versionName` - строка, видимая пользователю (можно использовать семантическое версионирование: 1.1, 1.2, 2.0)

### 2. Создание Keystore для подписи (ОБЯЗАТЕЛЬНО для продакшена)

Для тестирования можно использовать debug keystore, но для публикации в Google Play нужен свой keystore:

```bash
keytool -genkey -v -keystore dailyflow-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias dailyflow-key
```

**ВАЖНО**: 
- Сохраните keystore и пароли в безопасном месте! Без них нельзя будет обновлять приложение.
- Добавьте keystore в `.gitignore` (не коммитьте его в Git)

После создания keystore добавьте в `app/build.gradle.kts`:

```kotlin
android {
    // ...
    
    signingConfigs {
        create("release") {
            storeFile = file("${project.rootDir}/dailyflow-release.jks")
            storePassword = "ваш_пароль_keystore"
            keyAlias = "dailyflow-key"
            keyPassword = "ваш_пароль_ключа"
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
            signingConfig = signingConfigs.getByName("release")
        }
    }
}
```

**Альтернатива**: Используйте переменные окружения или `local.properties` для безопасности:

```kotlin
signingConfigs {
    create("release") {
        val keystorePropertiesFile = rootProject.file("keystore.properties")
        val keystoreProperties = java.util.Properties()
        if (keystorePropertiesFile.exists()) {
            keystoreProperties.load(java.io.FileInputStream(keystorePropertiesFile))
        }
        
        storeFile = file(keystoreProperties["storeFile"] as String? ?: "debug.keystore")
        storePassword = keystoreProperties["storePassword"] as String? ?: "android"
        keyAlias = keystoreProperties["keyAlias"] as String? ?: "androiddebugkey"
        keyPassword = keystoreProperties["keyPassword"] as String? ?: "android"
    }
}
```

Создайте файл `keystore.properties` (добавьте в `.gitignore`):
```properties
storeFile=dailyflow-release.jks
storePassword=ваш_пароль_keystore
keyAlias=dailyflow-key
keyPassword=ваш_пароль_ключа
```

### 3. Проверка миграций базы данных

Текущая версия БД: **7** (проверьте в `DailyFlowDatabase.kt`)

**Перед обновлением убедитесь:**
- ✅ Все миграции протестированы на реальных данных
- ✅ Если изменилась схема БД, увеличьте версию БД и добавьте новую миграцию
- ✅ `fallbackToDestructiveMigration()` - удалите для продакшена или используйте только в debug-сборках
- ✅ Пользователи с предыдущей версией смогут обновиться без потери данных

**Пример добавления миграции:**
```kotlin
@Database(
    entities = [...],
    version = 8,  // Увеличьте версию
    exportSchema = false
)

// Добавьте миграцию
private val MIGRATION_7_8 = object : Migration(7, 8) {
    override fun migrate(database: SupportSQLiteDatabase) {
        // Ваши SQL команды для миграции
        database.execSQL("ALTER TABLE tasks ADD COLUMN newColumn TEXT")
    }
}

// Добавьте в builder
.addMigrations(MIGRATION_7_8)
```

### 4. Сборка релизного APK

```bash
# Windows
.\gradlew assembleRelease

# Linux/Mac
./gradlew assembleRelease
```

APK будет в: `app/build/outputs/apk/release/app-release.apk`

**Для Google Play лучше использовать Android App Bundle (AAB):**
```bash
.\gradlew bundleRelease
```

AAB будет в: `app/build/outputs/bundle/release/app-release.aab`

### 5. Тестирование обновления

**Критично проверить:**
- ✅ Установка поверх старой версии (update scenario)
  - Установите старую версию (1.0)
  - Установите новую версию (1.1) поверх неё
  - Проверьте, что данные сохранились
- ✅ Миграция базы данных работает корректно
- ✅ Все новые функции работают
- ✅ Экспорт данных работает
- ✅ Напоминания работают
- ✅ Повторяющиеся задачи работают
- ✅ Нет крашей при запуске
- ✅ Производительность приемлемая

### 6. Проверка размера APK/AAB

```bash
# Проверить размер APK
ls -lh app/build/outputs/apk/release/app-release.apk

# Проверить размер AAB
ls -lh app/build/outputs/bundle/release/app-release.aab
```

**Рекомендации:**
- APK < 100MB для прямого скачивания
- Если APK > 100MB, используйте AAB (Google Play оптимизирует размер)
- Проверьте размер после ProGuard/R8 минификации

### 7. Подготовка релизных заметок

Создайте файл `CHANGELOG.md` с описанием изменений:

```markdown
# История изменений

## Версия 1.1 (2024-XX-XX)

### Новые функции
- Добавлен экспорт задач и заметок в текстовый файл
- Добавлены отчёты по выполнению задач
- Добавлена тепловая карта нагрузки по времени

### Улучшения
- Улучшена производительность экрана "День"
- Оптимизирована работа с повторяющимися задачами

### Исправления
- Исправлена ошибка при миграции базы данных
- Исправлена проблема с напоминаниями

### Известные проблемы
- (если есть)
```

### 8. Безопасность

Перед публикацией убедитесь:
- ✅ Нет хардкодных паролей/ключей в коде
- ✅ Keystore не закоммичен в Git (добавьте в `.gitignore`)
- ✅ ProGuard правила корректны (проверьте, что приложение работает после минификации)
- ✅ Экспорт данных безопасен (предупреждение пользователю о конфиденциальности)
- ✅ Нет логирования чувствительных данных

### 9. Финальная проверка

- ✅ Версия обновлена в `build.gradle.kts`
- ✅ Keystore создан и настроен
- ✅ Миграции БД протестированы
- ✅ APK/AAB собран успешно
- ✅ Обновление протестировано на реальном устройстве
- ✅ Релизные заметки подготовлены
- ✅ Код закоммичен в Git с понятным сообщением

## 📝 Следующее обновление

При следующем обновлении:
1. Увеличьте `versionCode` на 1 (следующее: 3)
2. Обновите `versionName` (например: "1.2" или "2.0" для major update)
3. Если изменилась схема БД, увеличьте версию БД и добавьте миграцию
4. Обновите `CHANGELOG.md`
5. Повторите все шаги из этого чеклиста

## ⚠️ Важные замечания

1. **Keystore критически важен** - без него нельзя обновлять приложение в Google Play. Сохраните его в безопасном месте!
2. **Миграции БД** - тестируйте на реальных данных перед релизом. Потеря данных пользователей недопустима.
3. **fallbackToDestructiveMigration()** - удалите для продакшена или используйте только в debug-сборках.
4. **Тестирование** - обязательно протестируйте обновление с предыдущей версии на новую.
5. **Backup** - перед обновлением убедитесь, что у вас есть backup кода и keystore.

## 🔗 Полезные ссылки

- [Android App Signing](https://developer.android.com/studio/publish/app-signing)
- [Version your app](https://developer.android.com/studio/publish/versioning)
- [Room Migrations](https://developer.android.com/training/data-storage/room/migrating-db-versions)
